const sideGraphDims = {
  svgWidth: 400,
  svgHeight: 50,
  marginLeft: 10,
  marginRight: 10,
  innerArea: 380,
};

export const { svgWidth, svgHeight, marginLeft, marginRight, innerArea } =
  sideGraphDims;
